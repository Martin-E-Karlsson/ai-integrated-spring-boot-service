# Copy to a local env file or export these in your shell / IDE run config.
# NEVER commit a populated .env file.

# OpenRouter API key — get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-replace-me
