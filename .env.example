# Create a new .env file at source root in the project
# Then add your own API key environment variable like in the example below

# OpenRouter API key — get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-replace-me